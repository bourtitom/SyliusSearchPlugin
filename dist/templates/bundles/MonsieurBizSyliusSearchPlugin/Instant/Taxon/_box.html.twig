<div class="result py-2 border-bottom">
    <div class="content">
        <a href="{{ path('monsieurbiz_sylius_search_taxon', {'slug': item.slug, '_locale': sylius.localeCode}) }}" class="header sylius-product-name">{{ item.name }}</a>
    </div>
</div>
