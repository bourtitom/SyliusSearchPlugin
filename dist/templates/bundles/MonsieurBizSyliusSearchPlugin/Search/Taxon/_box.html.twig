<div class="card h-100">
    <div class="card-body">
        <a href="{{ path('monsieurbiz_sylius_search_taxon', {'slug': item.slug, '_locale': sylius.localeCode}) }}" class="h5 sylius-product-name">{{ item.name }}</a>
    </div>
</div>
